<Page x:Class="CafeAutomation.Views.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Отчёты">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Отчёты" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="{Binding TodayRevenue}" FontSize="16" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding PopularDish}" FontSize="16" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding TablesStatus}" FontSize="16" Margin="0,10,0,0"/>

            <Button Content="Сформировать отчёт" Command="{Binding LoadReport}" Width="150" Height="30" Background="#007BFF" Foreground="White" Margin="0,30,0,0"/>
        </StackPanel>
    </Grid>
</Page>